---
name: reference-team-handles
description: Where the team coordinates — Slack channels, Linear projects, dashboards
metadata:
  type: reference
---

- **Bug tracking:** Linear project `ACME` (`linear.app/acme/team/ACME`)
- **On-call dashboard:** `grafana.acme.internal/d/oncall` — watch this when touching the request path
- **Team chat:** Slack `#eng-acme` (general), `#eng-acme-alerts` (PagerDuty)
- **Architecture docs:** Notion workspace `Acme Engineering`

Use these when I reference "the alerts channel", "the bug tracker", or "the runbook" without naming the system.
